# KTL 정도검사 계산기 — 에이전트 작업 규칙

> 상세 규칙은 `CLAUDE.md` 를 먼저 읽을 것. 아래는 그것과 무관하게 **항상 지켜야 하는** 규칙이다.

## 역할

너는 이 저장소에서 **범위가 한정된 작업자**다. 요청받은 범위만 수정한다.
배포·릴리스·인프라 변경은 사람이 판단한다. 먼저 하지 말고 제안만 한다.

## 절대 금지

- `vercel link` 로 새 프로젝트를 연결하지 않는다.
- `vercel --prod` 로 새 프로젝트를 만들지 않는다.
- `.vercel/project.json` 을 임의로 바꾸지 않는다. (과거 엉뚱한 GitHub 저장소에 연결돼 자동배포가 끊긴 사고가 있었다.)
- `api/` 파일에 비밀키를 하드코딩하지 않는다.
- `dist/`(빌드 산출물)와 `node_modules/` 를 편집하지 않는다.
- `console.log` 디버그 코드를 커밋하지 않는다.
- 승인 없이 `git push` 하지 않는다. push하면 Vercel로 자동 배포된다.

## 계산 정확성 — 이 저장소의 최우선 가치

사용자는 KTL 도메인 전문가이며 계산 정확성에 엄격하다. **추측하지 말고 엑셀 정본과 대조하라.**

- 기준값·수수료·계산정의의 **정본은 `Version11_(2026).xlsx`** 다. 엑셀은 **읽기 전용**이며, 앱이 여기에 쓰지 않는다.
- 기준값은 `sync-excel` 경로로만 갱신한다. 코드에 상수로 박지 말 것.
- **반복성** = 4개 콤보의 `MAX(STDEV)`. **드리프트** = 평균. 엑셀과 1:1로 일치해야 한다.
- 계산 공식을 수정하면 `npm test` (smoke + precision) 통과가 **필수**다. 새 계산 로직엔 테스트 케이스를 함께 추가한다.

## 판정 기준 (고시 별표 3)

직선성 ±2% · 제로드리프트 ±2% · 스판드리프트 ±5% · 반복성 ±2% · 응답시간 5분 이내

파라미터 9종: TOC, TN, TP, SS (mg/L) · pH · DO (mg/L) · COD (mg/L) · TU (NTU) · Cl (mg/L)

## 아키텍처

```
web/     → 프론트엔드 (HTML/CSS/JS)
src/     → 공유 비즈니스 로직 (브라우저·서버 양쪽 임포트) — 순수 함수 유지, 부수효과 금지
api/     → Vercel 서버리스 엔드포인트 (환경변수는 process.env.* 로만 접근)
```

- `src/excelClient.js` — 엑셀 파싱 싱글턴 (서버리스에서만 동작)
- `src/authService.js` — 인증 순수 함수 (브라우저·서버 공유)
- `src/calculator.js` — 측정 계산 핵심 로직

`src/` 를 고칠 땐 브라우저·서버 양쪽 사이드이펙트 경로를 확인한다. **"최소 변경"이 기본값이다.**

## 저장소 (오라클 아님)

- 기준값 = **엑셀 파일 읽기** (`Version11_(2026).xlsx`)
- 계산결과·챗로그 = **SQLite** (parser-photo-server, `studio.gool.uk` 터널 → :3333)
- 지식베이스 = 마크다운 + `search-index.json` (코사인 엣지가중치·IDF). 지식 문서를 고치면 `npm run gen:index` 로 인덱스를 재생성한다.

## 명령어

```bash
npm run dev      # 로컬 개발서버
npm run build    # 프로덕션 빌드
npm test         # smoke + precision 테스트
```

## 코드 품질

주석은 **WHY만** (WHAT은 코드가 설명한다). 한 함수 = 한 책임.
